services:
  - type: web
    name: finance-dashboard-api
    runtime: java
    buildCommand: ./mvnw clean package -DskipTests
    startCommand: java -jar target/financedashboardapi-0.0.1-SNAPSHOT.jar
    envVars:
      - key: MONGO_URI
        sync: false
      - key: JWT_SECRET
        sync: false